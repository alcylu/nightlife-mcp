---
phase: 09-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/admin/dashboardAuth.ts
  - src/admin/dashboardAuth.test.ts
  - src/admin/vipAdminRouter.ts
  - src/admin/vipDashboardPage.ts
  - src/services/vipAdmin.ts
  - src/services/vipAdmin.test.ts
  - src/http.ts
  - src/config.ts
  - src/services/venues.test.ts
  - src/services/performers.test.ts
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "src/admin/ directory no longer exists in nightlife-mcp"
    - "All /ops/* Express routes return 404 (or Express default 404)"
    - "All /api/v1/admin routes return 404 (or Express default 404)"
    - "Dashboard auth middleware and config fields are completely removed"
    - "All preserved routes still function (/health, /mcp, /api/v1, /deposit/*, /stripe/webhook)"
    - "TypeScript compiles with zero errors"
    - "All surviving tests pass"
  artifacts:
    - path: "src/http.ts"
      provides: "HTTP server with admin routes and imports removed"
      contains: "createStripeWebhookRouter"
      must_not_contain: "dashboardAuth|vipAdminRouter|vipDashboardPage|/ops/"
    - path: "src/config.ts"
      provides: "AppConfig without dashboard fields"
      must_not_contain: "vipDashboardAdmins|VipDashboardAdminCredential|parseVipDashboardAdmins|VIP_DASHBOARD_"
  key_links:
    - from: "src/http.ts"
      to: "src/routes/stripeWebhook.js"
      via: "import + app.use"
      pattern: "createStripeWebhookRouter"
    - from: "src/http.ts"
      to: "src/pages/depositResult.js"
      via: "import + app.get"
      pattern: "renderDeposit"
    - from: "src/services/venues.test.ts"
      to: "src/config.ts"
      via: "config mock matches AppConfig type"
      pattern: "stripeSecretKey"
---

<objective>
Remove all Express admin dashboard code from nightlife-mcp.

Purpose: The VIP booking admin dashboard has migrated to nlt-admin (Next.js). The Express-based admin UI, API routes, auth middleware, and service layer in nightlife-mcp are dead code. Removing them reduces attack surface, eliminates maintenance burden, and leaves nightlife-mcp as a clean MCP server + REST API.

Output: nightlife-mcp with zero admin dashboard code, passing TypeScript checks and all surviving tests.
</objective>

<execution_context>
@/Users/alcylu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alcylu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cleanup/09-RESEARCH.md

Key files to modify:
@src/http.ts
@src/config.ts
@src/services/venues.test.ts
@src/services/performers.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete admin files and service layer</name>
  <files>
    src/admin/dashboardAuth.ts,
    src/admin/dashboardAuth.test.ts,
    src/admin/vipAdminRouter.ts,
    src/admin/vipDashboardPage.ts,
    src/services/vipAdmin.ts,
    src/services/vipAdmin.test.ts
  </files>
  <action>
Delete the entire `src/admin/` directory (4 files) and the admin-only service files:

1. `rm -rf src/admin/` — removes dashboardAuth.ts, dashboardAuth.test.ts, vipAdminRouter.ts, vipDashboardPage.ts
2. `rm src/services/vipAdmin.ts` — admin service layer (775 lines), imported ONLY by vipAdminRouter.ts
3. `rm src/services/vipAdmin.test.ts` — admin service tests (456 lines)

DO NOT delete any of these (they are used by non-admin code paths):
- `src/services/deposits.ts` — used by vipBookings.ts (MCP tool) and stripeWebhook route
- `src/services/email.ts` — used by vipBookings.ts and stripeWebhook route
- `src/services/stripe.ts` — used by stripeWebhook route
- `src/routes/stripeWebhook.ts` — live customer-facing webhook
- `src/pages/depositResult.ts` — live customer-facing deposit result pages
  </action>
  <verify>
    <automated>test ! -d src/admin/ && test ! -f src/services/vipAdmin.ts && test ! -f src/services/vipAdmin.test.ts && echo "PASS: All admin files deleted" || echo "FAIL: Some admin files remain"</automated>
  </verify>
  <done>The 6 admin-only files are deleted. src/admin/ directory no longer exists. src/services/deposits.ts, email.ts, and stripe.ts are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove admin references from http.ts and config.ts, fix test mocks</name>
  <files>src/http.ts, src/config.ts, src/services/venues.test.ts, src/services/performers.test.ts</files>
  <action>
**src/http.ts** — Surgically remove all admin wiring. Remove these specific sections:

1. Remove the 3 admin import lines (currently lines 28-33):
   ```
   import { createDashboardAuth, type RequestWithDashboardAuth } from "./admin/dashboardAuth.js";
   import { createVipAdminRouter } from "./admin/vipAdminRouter.js";
   import { renderVipDashboardLoginPage, renderVipDashboardPage } from "./admin/vipDashboardPage.js";
   ```

2. Remove the `dashboardAuth` construction block (currently lines 636-641):
   ```
   const dashboardAuth = createDashboardAuth({
     admins: config.vipDashboardAdmins,
     sessionTtlMinutes: config.vipDashboardSessionTtlMinutes,
     sessionCookieName: config.vipDashboardSessionCookieName,
     secureCookies: process.env.NODE_ENV === "production",
   });
   ```

3. Remove `express.urlencoded` middleware (line 643) — only used by the /ops/login POST form; no other route needs URL-encoded body parsing:
   ```
   app.use(express.urlencoded({ extended: false }));
   ```

4. Remove all `/ops/*` route handlers (lines 765-825):
   - `GET /ops/login`
   - `POST /ops/login`
   - `POST /ops/logout`
   - `GET /ops/vip-dashboard`

5. Remove the `/api/v1/admin` mount block (lines 855-864):
   ```
   app.use(
     "/api/v1/admin",
     (req: RequestWithDashboardAuth, res, next) =>
       dashboardAuth.requireApiSession(req, res, next),
     createVipAdminRouter(supabase, { ... }),
   );
   ```

PRESERVE everything else:
- `/deposit/success` and `/deposit/cancelled` routes (customer-facing)
- `/stripe/webhook` route (live payment webhook)
- All `/mcp` routes (core product)
- All `/api/v1` routes (REST API for external consumers)
- `/health` endpoint
- `/debug/recommendations` (dev-only)
- `/favicon.ico`
- `express.json()` middleware (used by MCP and REST routes)

**src/config.ts** — Remove all dashboard-related items:

1. In `envSchema`: remove these 3 fields:
   - `VIP_DASHBOARD_ADMINS: z.string().optional()`
   - `VIP_DASHBOARD_SESSION_TTL_MINUTES: z.coerce.number().int().min(5).max(10080).default(720)`
   - `VIP_DASHBOARD_SESSION_COOKIE_NAME: z.string().default("vip_dashboard_session")`

2. Remove the exported type `VipDashboardAdminCredential` (lines 45-48)

3. In `AppConfig` type: remove these 3 fields:
   - `vipDashboardAdmins: VipDashboardAdminCredential[]`
   - `vipDashboardSessionTtlMinutes: number`
   - `vipDashboardSessionCookieName: string`

4. Remove the entire `parseVipDashboardAdmins()` function (lines 74-104)

5. In `loadConfig()` return object: remove the `vipDashboardAdmins` variable assignment (line 116) and the 3 dashboard fields from the return object (lines 134-136)

PRESERVE: All Stripe, Resend, and other non-dashboard config fields.

**src/services/venues.test.ts** — Remove the 3 stale dashboard mock lines from the config mock object (currently lines 31-33):
```
vipDashboardAdmins: [],
vipDashboardSessionTtlMinutes: 720,
vipDashboardSessionCookieName: "vip_dashboard_session",
```

**src/services/performers.test.ts** — Same removal of 3 stale dashboard mock lines (currently lines 30-32).
  </action>
  <verify>
    <automated>cd /Users/alcylu/Apps/nightlife-mcp && npm run check && npm test</automated>
  </verify>
  <done>TypeScript compiles with zero errors (npm run check passes). All surviving tests pass (npm test passes). No reference to dashboardAuth, vipAdminRouter, vipDashboardPage, VIP_DASHBOARD_ADMINS, or /ops/ remains in http.ts or config.ts. Stripe/Resend config and deposit/webhook routes are preserved.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification sequence:

1. **TypeScript check:** `npm run check` — zero errors confirms no broken imports or type mismatches
2. **Test suite:** `npm test` — all surviving tests pass; deleted test files (vipAdmin.test.ts, dashboardAuth.test.ts) no longer run
3. **File absence:** `ls src/admin/ 2>&1` returns "No such file or directory"
4. **No dead references:** `grep -r "dashboardAuth\|vipAdminRouter\|vipDashboardPage\|VIP_DASHBOARD_ADMINS\|/ops/login\|/ops/logout\|/ops/vip-dashboard\|/api/v1/admin" src/` returns no matches
5. **Preserved routes intact:** `grep -c "deposit/success\|deposit/cancelled\|stripe/webhook\|/health\|/api/v1" src/http.ts` returns expected count (all preserved)

Post-deploy ops note (not a code task): After this code ships to Railway production, the following env vars can be removed from the nightlife-mcp Railway service:
- `VIP_DASHBOARD_ADMINS`
- `VIP_DASHBOARD_SESSION_TTL_MINUTES`
- `VIP_DASHBOARD_SESSION_COOKIE_NAME`
</verification>

<success_criteria>
- src/admin/ directory does not exist
- src/services/vipAdmin.ts and src/services/vipAdmin.test.ts do not exist
- `npm run check` passes with zero TypeScript errors
- `npm test` passes — all surviving tests green
- No grep matches for admin dashboard references in src/
- Deposit, Stripe webhook, MCP, REST API, and health routes are all preserved in http.ts
</success_criteria>

<output>
After completion, create `.planning/phases/09-cleanup/09-01-SUMMARY.md`
</output>
