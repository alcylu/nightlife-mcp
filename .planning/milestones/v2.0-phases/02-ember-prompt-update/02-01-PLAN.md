---
phase: 02-ember-prompt-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/alcylu/Apps/openclaw/sync/instances/ember/workspace/skills/nightlife-concierge/SKILL.md
  - /Users/alcylu/Apps/openclaw/sync/instances/mamad/workspace/skills/nightlife-concierge/SKILL.md
  - /Users/alcylu/Apps/openclaw/sync/instances/lisa/workspace/skills/nightlife-concierge/SKILL.md
  - /Users/alcylu/Apps/openclaw/sync/instances/lisa/workspace-template/skills/nightlife-concierge/SKILL.md
autonomous: false
requirements: [EMBR-01, EMBR-02, EMBR-03]

must_haves:
  truths:
    - "Ember SKILL.md contains zero references to get_vip_table_availability or get_vip_table_chart"
    - "Ember SKILL.md references get_vip_pricing at least 3 times (Tool Contract, VIP Booking Flow, VIP Pricing Freshness Rule)"
    - "Ember SKILL.md contains a mandatory confirmation gate that prevents auto-submission of booking requests"
    - "Ember SKILL.md contains a CRITICAL guardrail stating the table chart is layout reference only with no availability inference"
    - "All four generic SKILL.md files (ember, mamad, lisa/workspace, lisa/workspace-template) are byte-for-byte identical"
    - "Updated SKILL.md is deployed to ember, mamad, and lisa Railway containers via oc-sync push"
  artifacts:
    - path: "/Users/alcylu/Apps/openclaw/sync/instances/ember/workspace/skills/nightlife-concierge/SKILL.md"
      provides: "Rewritten VIP flow using get_vip_pricing with confirmation gate and chart guardrail"
      contains: "get_vip_pricing"
    - path: "/Users/alcylu/Apps/openclaw/sync/instances/mamad/workspace/skills/nightlife-concierge/SKILL.md"
      provides: "Identical copy of ember SKILL.md"
      contains: "get_vip_pricing"
    - path: "/Users/alcylu/Apps/openclaw/sync/instances/lisa/workspace/skills/nightlife-concierge/SKILL.md"
      provides: "Identical copy of ember SKILL.md"
      contains: "get_vip_pricing"
    - path: "/Users/alcylu/Apps/openclaw/sync/instances/lisa/workspace-template/skills/nightlife-concierge/SKILL.md"
      provides: "Identical copy of ember SKILL.md"
      contains: "get_vip_pricing"
  key_links:
    - from: "SKILL.md VIP Booking Flow"
      to: "get_vip_pricing MCP tool"
      via: "Step 2 instruction to call get_vip_pricing"
      pattern: "Call `get_vip_pricing`"
    - from: "SKILL.md VIP Booking Flow step 7"
      to: "create_vip_booking_request"
      via: "Mandatory confirmation gate before tool call"
      pattern: "MANDATORY CONFIRMATION GATE"
    - from: "SKILL.md VIP Table Chart section"
      to: "get_vip_pricing layout_image_url field"
      via: "Chart retrieval via single tool call"
      pattern: "LAYOUT REFERENCE ONLY"
---

<objective>
Rewrite the VIP-related sections of the nightlife-concierge SKILL.md to replace the old two-tool flow (get_vip_table_availability + get_vip_table_chart) with the new single get_vip_pricing tool, add a mandatory user confirmation gate before booking submission, and add a CRITICAL guardrail that the table chart shows layout only (not availability). Deploy to all generic openclaw instances.

Purpose: Phase 1 deployed the new get_vip_pricing MCP tool. Now Ember (and all generic nightlife concierge instances) must be instructed to use it instead of the old tools, with stronger behavioral guardrails around booking confirmation and chart interpretation.

Output: Updated SKILL.md deployed to ember, mamad, and lisa containers. All four local copies are identical.
</objective>

<execution_context>
@/Users/alcylu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alcylu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ember-prompt-update/02-RESEARCH.md
@.planning/phases/01-mcp-pricing-tool/01-02-SUMMARY.md

@/Users/alcylu/Apps/openclaw/sync/instances/ember/workspace/skills/nightlife-concierge/SKILL.md

<interfaces>
<!-- The executor needs the exact current SKILL.md content and the exact replacement text for each section. -->
<!-- The research file (02-RESEARCH.md) contains complete replacement text for every affected section. -->
<!-- Key sections from the research Code Examples that the executor should use verbatim: -->

Section replacements (from 02-RESEARCH.md "Code Examples" and "Architecture Patterns"):

1. YAML frontmatter description (line 2):
   OLD: "Query Nightlife MCP for events, VIP booking, table availability/chart, guest list, and venue info for any Tokyo venue."
   NEW: "Query Nightlife MCP for events, VIP pricing, booking requests, guest list, and venue info for any Tokyo venue."

2. Tool Contract > VIP Booking (lines 49-54):
   OLD: 5 tools including get_vip_table_availability and get_vip_table_chart
   NEW: 4 tools — replace those two with single get_vip_pricing line

3. VIP Booking Flow > Flow (lines 72-90):
   OLD: Steps 2-5 use get_vip_table_availability + get_vip_table_chart, step 8 auto-submits
   NEW: Step 2 uses get_vip_pricing, step 7 is MANDATORY CONFIRMATION GATE, step 8 only after YES

4. VIP Pricing Freshness Rule (lines 141-147):
   OLD: References get_vip_table_availability
   NEW: References get_vip_pricing, changes "same date" to "same venue"

5. VIP Table Chart (lines 149-159):
   OLD: Calls get_vip_table_chart + get_vip_table_availability, no guardrail
   NEW: CRITICAL layout-only guardrail, calls get_vip_pricing only

6. VIP Presentation Rule (lines 161-170):
   OLD: Good example says "2 tables available"
   NEW: Good example uses pricing-range language only

7. Venue Lookup (line 26):
   OLD: References get_vip_table_availability
   NEW: References get_vip_pricing

8. Venue Knowledge (line 199):
   OLD: References get_vip_table_availability
   NEW: References get_vip_pricing

9. Booking Tone (lines 65-70):
   OLD: "Prices from `min_spend` are indicative"
   NEW: "Prices from `get_vip_pricing` are indicative ranges"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md VIP sections and sync to all instances</name>
  <files>
    /Users/alcylu/Apps/openclaw/sync/instances/ember/workspace/skills/nightlife-concierge/SKILL.md
    /Users/alcylu/Apps/openclaw/sync/instances/mamad/workspace/skills/nightlife-concierge/SKILL.md
    /Users/alcylu/Apps/openclaw/sync/instances/lisa/workspace/skills/nightlife-concierge/SKILL.md
    /Users/alcylu/Apps/openclaw/sync/instances/lisa/workspace-template/skills/nightlife-concierge/SKILL.md
  </files>
  <action>
Read the current ember SKILL.md (223 lines). Apply ALL of the following section rewrites. Use the exact replacement text from the research file's Code Examples section where provided. For sections not fully written out in research, apply the targeted changes described below.

**Changes to apply (in file order):**

1. **YAML frontmatter** (line 2): Change description from "table availability/chart" to "VIP pricing, booking requests". New value: `"Query Nightlife MCP for events, VIP pricing, booking requests, guest list, and venue info for any Tokyo venue."`

2. **Venue Lookup** (line 26): Replace `get_vip_table_availability` with `get_vip_pricing`. New text: "Use `search_venues`, `get_venue_info`, `get_vip_pricing`, and other MCP tools to get live data."

3. **Tool Contract > VIP Booking** (lines 49-54): Remove `get_vip_table_availability` and `get_vip_table_chart` lines. Add: `- \`get_vip_pricing\` — VIP pricing ranges, zone summaries, table chart URL, and booking affordance`

4. **VIP Booking Flow > Booking Tone** (line 69): Change "Prices from \`min_spend\` are indicative" to "Prices from \`get_vip_pricing\` are indicative ranges — \"weekday minimums start around ¥X, subject to change\""

5. **VIP Booking Flow > Flow** (lines 74-90): Replace entire flow with the 10-step flow from research Code Examples. Key changes:
   - Step 2: Call `get_vip_pricing` (not get_vip_table_availability), date is optional ("include `date` if the user mentioned a specific date")
   - Step 3: Remove get_vip_table_chart entirely, keep get_venue_info
   - Step 4: Present pricing conversationally + chart URL if available
   - Step 7: **MANDATORY CONFIRMATION GATE** — "Ask explicitly — \"Would you like me to submit an inquiry?\" — and wait for the user's YES before proceeding. Do NOT call `create_vip_booking_request` until you have explicit confirmation."
   - Step 8: Call create_vip_booking_request only after confirmed YES
   - Step 9: Share booking_request_id + tentative language

6. **VIP Pricing Freshness Rule** (lines 141-147): Replace entire section with the version from research Code Examples. Key change: tool name to `get_vip_pricing`, "same date" to "same venue".

7. **VIP Table Chart** (lines 149-159): Replace entire section with the version from research Code Examples. Add CRITICAL guardrail as first line: "The table chart is a LAYOUT REFERENCE ONLY." Steps now use `get_vip_pricing` only (no separate chart tool). Add explicit "Never infer table availability from the image."

8. **VIP Presentation Rule > Good example** (lines 165-166): Replace "2 tables available (2-8 people, from ~¥55K weekday / ~¥110K weekend)" with "weekday minimums from ~¥55K, weekend from ~¥110K. Want me to put in a request?" Also replace Bad example to add note about "available" implying live status. Use the exact text from research Code Examples.

9. **Venue Knowledge** (line 199): Replace `get_vip_table_availability` with `get_vip_pricing`. New text: "For ANY venue question, always call MCP tools first (`get_venue_info`, `search_venues`, `get_vip_pricing`)."

**After editing ember SKILL.md:**
- Copy the ember file byte-for-byte to mamad, lisa/workspace, and lisa/workspace-template paths.
- DO NOT modify oneoak — it has a venue-specific variant that is intentionally different.

**Verification before proceeding:**
- `grep -c "get_vip_table_availability\|get_vip_table_chart" SKILL.md` must return 0 for all 4 files
- `grep -c "get_vip_pricing" SKILL.md` must return >= 3 for all 4 files
- `grep -c "CONFIRMATION GATE\|MANDATORY.*CONFIRM\|explicitly confirms" SKILL.md` must return >= 1
- `grep -c "LAYOUT REFERENCE ONLY" SKILL.md` must return >= 1
- `diff ember/...SKILL.md mamad/...SKILL.md` must be empty
- `diff ember/...SKILL.md lisa/workspace/...SKILL.md` must be empty
- `diff ember/...SKILL.md lisa/workspace-template/...SKILL.md` must be empty
  </action>
  <verify>
    <automated>
# All checks must pass (returns 0 matches for old tools, >= 3 for new tool, >= 1 for guardrails, empty diffs):
EMBER="/Users/alcylu/Apps/openclaw/sync/instances/ember/workspace/skills/nightlife-concierge/SKILL.md" && \
MAMAD="/Users/alcylu/Apps/openclaw/sync/instances/mamad/workspace/skills/nightlife-concierge/SKILL.md" && \
LISA="/Users/alcylu/Apps/openclaw/sync/instances/lisa/workspace/skills/nightlife-concierge/SKILL.md" && \
LISAT="/Users/alcylu/Apps/openclaw/sync/instances/lisa/workspace-template/skills/nightlife-concierge/SKILL.md" && \
echo "Old tools (must be 0):" && grep -c "get_vip_table_availability\|get_vip_table_chart" "$EMBER" || true && \
echo "New tool count (must be >= 3):" && grep -c "get_vip_pricing" "$EMBER" && \
echo "Confirmation gate (must be >= 1):" && grep -ci "MANDATORY.*CONFIRM\|CONFIRMATION GATE\|explicitly confirms" "$EMBER" && \
echo "Chart guardrail (must be >= 1):" && grep -c "LAYOUT REFERENCE ONLY" "$EMBER" && \
echo "Diff ember vs mamad:" && diff "$EMBER" "$MAMAD" && echo "IDENTICAL" && \
echo "Diff ember vs lisa:" && diff "$EMBER" "$LISA" && echo "IDENTICAL" && \
echo "Diff ember vs lisa-template:" && diff "$EMBER" "$LISAT" && echo "IDENTICAL"
    </automated>
  </verify>
  <done>
All four generic SKILL.md files are updated with: (1) get_vip_pricing replacing old tools everywhere, (2) mandatory confirmation gate in VIP Booking Flow step 7, (3) CRITICAL layout-only guardrail in VIP Table Chart section, (4) updated VIP Presentation Rule example without "available" language, (5) updated YAML description. All four files are byte-for-byte identical. Zero references to old tool names remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to Railway and verify live Ember behavior</name>
  <files>
    /Users/alcylu/Apps/openclaw/sync/instances/ember/workspace/skills/nightlife-concierge/SKILL.md
    /Users/alcylu/Apps/openclaw/sync/instances/mamad/workspace/skills/nightlife-concierge/SKILL.md
    /Users/alcylu/Apps/openclaw/sync/instances/lisa/workspace/skills/nightlife-concierge/SKILL.md
  </files>
  <action>
Deploy updated SKILL.md to all generic openclaw Railway containers and clear sessions so new skill takes effect:

1. Source Railway tokens: `source ~/.railway/tokens.env`
2. cd to sync directory: `cd /Users/alcylu/Apps/openclaw/sync`
3. Push to ember: `./oc-sync push ember workspace/skills/nightlife-concierge/SKILL.md`
4. Push to mamad: `./oc-sync push mamad workspace/skills/nightlife-concierge/SKILL.md`
5. Push to lisa: `./oc-sync push lisa workspace/skills/nightlife-concierge/SKILL.md`
6. Push lisa workspace-template (verify path with `./oc-sync ls lisa` first)
7. Clear sessions on each instance if oc-sync does not auto-restart (check oc-sync output for restart confirmation)

Then pause for human verification of live Ember behavior.
  </action>
  <verify>
Human verifies live Ember via LINE or Discord:

1. **EMBR-01 test:** Send "What are the VIP options at Zouk?" — Ember should call `get_vip_pricing` (not `get_vip_table_availability`). Verify the response mentions pricing ranges (weekday/weekend minimums), not per-table availability.

2. **EMBR-02 test:** Walk through a VIP booking flow to the point of submission. Ember must ask "Would you like me to submit an inquiry?" (or similar explicit question) and WAIT for your yes before calling `create_vip_booking_request`. Do NOT say yes — just verify the pause happens.

3. **EMBR-03 test:** Ask "Can you show me the table layout for CE LA VI?" — Ember should share the chart URL but NOT say anything like "Table X appears available." The response should note the chart is a layout/seating reference.

If any test fails, describe what Ember actually did so we can diagnose.

Resume signal: Type "approved" if all 3 tests pass, or describe what failed.
  </verify>
  <done>All three live Ember tests pass: (1) VIP inquiry triggers get_vip_pricing with pricing-range language, (2) booking flow pauses for explicit user confirmation before submission, (3) chart is presented as layout reference without availability inference.</done>
</task>

</tasks>

<verification>
Phase 2 is complete when:
1. All four generic SKILL.md files contain get_vip_pricing and zero references to old tools
2. Confirmation gate language is present in VIP Booking Flow
3. Chart layout-only guardrail is present with CRITICAL marker
4. Files are deployed to Railway containers
5. Live Ember test confirms new behavior (human-verified)
</verification>

<success_criteria>
- grep for "get_vip_table_availability" and "get_vip_table_chart" returns 0 matches across all 4 SKILL.md files
- grep for "get_vip_pricing" returns >= 3 matches in each file
- grep for "MANDATORY CONFIRMATION GATE" or equivalent returns >= 1 match
- grep for "LAYOUT REFERENCE ONLY" returns >= 1 match
- All 4 files are byte-for-byte identical (diff returns empty)
- Live Ember responds with pricing ranges (not per-table status) on VIP inquiry
- Live Ember asks for confirmation before submitting booking request
- Live Ember does not infer availability from table chart image
</success_criteria>

<output>
After completion, create `.planning/phases/02-ember-prompt-update/02-01-SUMMARY.md`
</output>
